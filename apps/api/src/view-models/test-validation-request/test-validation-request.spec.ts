import { TestValidationRequest } from './test-validation-request';

describe('TestValidationRequest', () => {
  it('should be defined', () => {
    expect(new TestValidationRequest()).toBeDefined();
  });
});
